<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>
<match **>
  @type copy
  <store>
    @type newrelic
    license_key NEW_RELIC_LICENSE_KEY
  </store>
  <store>
    @type stdout
  </store>
</match>